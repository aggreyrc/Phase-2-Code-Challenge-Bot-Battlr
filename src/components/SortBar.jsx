// src/components/SortBar.js
import React from 'react';

const SortBar = ({ onSort }) => {
  return (
    <div>
      <h3>Sort Bots By:</h3>
      {/* Include buttons for sorting by health, damage, and armor */}
    </div>
  );
};

export default SortBar;
